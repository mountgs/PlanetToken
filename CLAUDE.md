# CLAUDE.md — Project Conventions for new-api

@AGENTS.md

## Claude Code

- Follow the shared project instructions imported from `AGENTS.md`.

## Agent Harness

See `AGENTS.md` (Agent Harness section). Run `./init.sh` before coding. Track work in `feature_list.json` and `progress.md`.
